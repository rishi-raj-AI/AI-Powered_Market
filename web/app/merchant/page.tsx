'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {Store,PackageCheck,IndianRupee,ClipboardList} from 'lucide-react';import {gaonApi,Merchant} from '@/lib/api';import {Nav} from '@/components/Nav';
export default function MerchantPage(){const [m,setM]=useState<Merchant|null>(null);const [err,setErr]=useState('');useEffect(()=>{gaonApi.merchant().then(setM).catch(e=>setErr(e.message))},[]);return <><Nav/><div className="dashboard"><aside className="sidebar"><strong>Merchant workspace</strong><Link href="/merchant">Overview</Link><Link href="/market">Catalogue preview</Link><Link href="/orders">Orders</Link></aside><main className="main"><div className="sectionHead"><div><span className="eyebrow">Merchant console</span><h2>{m?.business_name||'Your business'}</h2></div>{m&&<span className="badge">{m.status}</span>}</div>{err&&<div className="notice">{err}</div>}<div className="grid"><div className="card"><Store/><h3>Store setup</h3><p className="muted">Village, landmark and delivery settings.</p></div><div className="card"><PackageCheck/><h3>Inventory</h3><p className="muted">Manage prices, stock and availability.</p></div><div className="card"><ClipboardList/><h3>Orders</h3><p className="muted">Accept, prepare and mark ready.</p></div><div className="card"><IndianRupee/><h3>Sales</h3><p className="muted">Settlement analytics coming next.</p></div></div></main></div></>}
