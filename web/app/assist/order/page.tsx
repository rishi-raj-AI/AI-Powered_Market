'use client';
import {useState} from 'react';import {Nav} from '@/components/Nav';import {getToken} from '@/lib/api';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000/api/v1';
export default function OrderAssist(){const[text,setText]=useState('');const[language,setLanguage]=useState<'en'|'hi'|'mr'>('en');const[result,setResult]=useState<any>(null);const[error,setError]=useState('');async function run(){try{const token=getToken();if(!token)throw new Error('Login required');const r=await fetch(`${API}/ai/order-intent`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${token}`},body:JSON.stringify({text,language,input_mode:'text'})});if(!r.ok)throw new Error(`Intent request failed (${r.status})`);setResult(await r.json());setError('')}catch(e:any){setError(e.message)}}return <><Nav/><main className="container section"><span className="eyebrow">Deterministic multilingual assist</span><h2>Describe what you need</h2><p className="muted">Use English, Hindi or Marathi. This parser structures your request; it does not place an order or change your cart.</p><div className="panel stack"><select value={language} onChange={e=>setLanguage(e.target.value as any)}><option value="en">English</option><option value="hi">Hindi</option><option value="mr">Marathi</option></select><textarea rows={5} value={text} onChange={e=>setText(e.target.value)} placeholder="e.g. 2 kg onions and one packet of tea"/><button className="btn" disabled={!text.trim()} onClick={run}>Understand request</button>{error&&<div className="notice">{error}</div>}{result&&<pre style={{whiteSpace:'pre-wrap'}}>{JSON.stringify(result,null,2)}</pre>}</div></main></>}
