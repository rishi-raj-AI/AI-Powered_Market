'use client';
import {useEffect,useState} from 'react';import {gaonApi,Cart} from '@/lib/api';import {Nav} from '@/components/Nav';
export default function CartPage(){const [cart,setCart]=useState<Cart|null>(null);const [err,setErr]=useState('');useEffect(()=>{gaonApi.cart().then(setCart).catch(e=>setErr(e.message))},[]);return <><Nav/><main className="container section"><div className="sectionHead"><div><span className="eyebrow">Basket</span><h2>Your cart</h2></div></div>{err&&<div className="notice">{err}</div>}<div className="panel">{cart?.items?.length?cart.items.map(i=><div className="row space" style={{padding:'12px 0',borderBottom:'1px solid var(--line)'}} key={i.id}><div><strong>{i.store_product?.product?.name||'Item'}</strong><div className="muted">Qty {i.quantity}</div></div><strong>₹{i.unit_price}</strong></div>):<p className="muted">Your cart is empty.</p>}</div></main></>}
