'use client';
import {useEffect,useState} from 'react';import {Boxes,ChartNoAxesCombined,MapPinned,Store,Truck,Users} from 'lucide-react';import {gaonApi,Merchant,Store as StoreType,Village} from '@/lib/api';import {Nav} from '@/components/Nav';
export default function Admin(){const [merchants,setMerchants]=useState<Merchant[]>([]);const [stores,setStores]=useState<StoreType[]>([]);const [villages,setVillages]=useState<Village[]>([]);const [msg,setMsg]=useState('');async function load(){try{const [m,s,v]=await Promise.all([gaonApi.merchants(),gaonApi.stores(),gaonApi.villages()]);setMerchants(m);setStores(s);setVillages(v);setMsg('')}catch(e:any){setMsg(e.message)}}useEffect(()=>{load()},[]);async function approve(id:string){try{await gaonApi.approveMerchant(id);setMsg('Merchant approved.');await load()}catch(e:any){setMsg(e.message)}}const pending=merchants.filter(m=>m.status==='pending');return <><Nav/><main className="container section"><div className="sectionHead"><div><span className="eyebrow">Operations control</span><h2>Admin dashboard</h2></div></div>{msg&&<div className="notice">{msg}</div>}<div className="grid"><div className="card"><Store/><h3>{merchants.length} merchants</h3><p className="muted">{pending.length} awaiting approval</p></div><div className="card"><MapPinned/><h3>{villages.length} villages</h3><p className="muted">Active marketplace coverage</p></div><div className="card"><Boxes/><h3>{stores.length} stores</h3><p className="muted">Active local storefronts</p></div><div className="card"><Truck/><h3>Delivery network</h3><p className="muted">Ready-order dispatch enabled</p></div></div><section className="section"><div className="panel"><div className="sectionHead"><div><h2 style={{fontSize:24}}>Merchant approvals</h2><span className="muted">Verify local businesses before activation.</span></div></div><table className="table"><thead><tr><th>Business</th><th>Status</th><th>GSTIN</th><th>Action</th></tr></thead><tbody>{merchants.map(m=><tr key={m.id}><td><strong>{m.business_name}</strong></td><td><span className="badge">{m.status}</span></td><td>{m.gstin||'—'}</td><td>{m.status==='pending'?<button className="btn" onClick={()=>approve(m.id)}>Approve</button>:<span className="muted">Active</span>}</td></tr>)}{merchants.length===0&&<tr><td colSpan={4} className="muted">No merchant applications yet.</td></tr>}</tbody></table></div></section><section className="section"><div className="panel"><h3>Platform readiness</h3><table className="table"><tbody><tr><td>Authentication & roles</td><td><span className="badge">Live</span></td></tr><tr><td>Marketplace & inventory</td><td><span className="badge">Live</span></td></tr><tr><td>Cart, checkout & orders</td><td><span className="badge">Live</span></td></tr><tr><td>Merchant fulfilment & delivery</td><td><span className="badge">Live</span></td></tr><tr><td>Real SMS / UPI / Maps / Push</td><td><span className="badge">Credentials pending</span></td></tr></tbody></table></div></section></main></>}
