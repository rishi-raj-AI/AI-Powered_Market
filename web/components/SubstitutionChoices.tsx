'use client';
import {useState} from 'react';
import {RefreshCw} from 'lucide-react';
import {gaonApi,Substitution} from '@/lib/api';

export function SubstitutionChoices({listingId,onAdded}:{listingId:string;onAdded?:()=>void}){const[open,setOpen]=useState(false);const[items,setItems]=useState<Substitution[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState('');async function toggle(){if(open){setOpen(false);return}setOpen(true);if(items.length)return;setLoading(true);try{setItems(await gaonApi.substitutions(listingId));setError('')}catch(e:any){setError(e.message)}finally{setLoading(false)}}async function add(item:Substitution){setBusy(item.listing_id);try{await gaonApi.addCart(item.listing_id,1);onAdded?.()}catch(e:any){setError(e.message)}finally{setBusy('')}}return <div style={{marginTop:8}}><button className="btn ghost" onClick={toggle}><RefreshCw size={14}/>{open?'Hide alternatives':'Alternatives'}</button>{open&&<div className="notice" style={{marginTop:8}}><strong>Choose a replacement manually</strong><div className="muted small">Nothing is substituted automatically. Checkout revalidates your chosen item.</div>{loading&&<div className="muted small">Checking same-store alternatives…</div>}{error&&<div className="dangerText small">{error}</div>}{!loading&&!items.length&&!error&&<div className="muted small">No same-store alternative is available.</div>}{items.slice(0,3).map(item=><div className="row space" key={item.listing_id} style={{marginTop:8}}><span><strong>{item.name}</strong><span className="muted small"> • {item.unit} • ₹{item.price}</span></span><button className="btn secondary" disabled={busy===item.listing_id} onClick={()=>add(item)}>{busy===item.listing_id?'Adding…':'Choose'}</button></div>)}</div>}</div>}
