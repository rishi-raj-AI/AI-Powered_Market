'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import {gaonApi,clearToken,User} from '@/lib/api';
export function Nav(){const [me,setMe]=useState<User|null>(null);useEffect(()=>{gaonApi.me().then(setMe).catch(()=>{})},[]);return <header className="topbar"><div className="container nav"><Link href="/" className="brand">Gaon<span>One</span></Link><nav className="navlinks"><Link href="/market">Explore</Link><Link href="/orders">Orders</Link>{me?.role==='merchant'&&<Link href="/merchant">Merchant</Link>}{me?.role==='admin'&&<Link href="/admin">Admin</Link>}{me?<button className="btn ghost" onClick={()=>{clearToken();location.href='/'}}>Logout</button>:<Link className="btn" href="/login">Login</Link>}</nav></div></header>}
